<%# Replaces the thinking indicator when the interview is complete. UI shows review panel (Step 11). %>
<div id="architect_thinking_indicator" data-architect-status="complete" data-session-id="<%= session.id %>" class="architect-complete-marker hidden">
</div>
